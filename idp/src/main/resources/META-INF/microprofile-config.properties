# idp config defaults, shipped from this library jar. Library jars ship defaults as
# META-INF/microprofile-config.properties (ordinal 100) — a jar's application.properties is ignored
# — read by whichever app pulls the jar in. The consuming app / env overrides any of these.
#
# idp owns its OWN named datasource + persistence unit + Flyway lineage: a postgres database of its
# own. Nothing else may share it: the signing key lives there, and that database is the only reason
# a token issued before a restart still verifies after one.
#
# THE THREE KEYS BELOW ARE THE PLATFORM'S GENERIC RESOURCE CONTRACT. A repository declares
# `resources: postgresql:db` in .config/qits/deployments.yml; qits-platform-deployments creates the
# role and the database before the successor container starts and injects QITS_RESOURCE_DB_URL /
# _USERNAME / _PASSWORD into it. Nothing here is idp-specific — an application maps the generic
# variables in its own shipped defaults, which is what keeps the deployer framework-agnostic. This
# service is a CORE SEED service, so at bootstrap the same triple arrives from the CLI instead: it
# provisions the database over plain JDBC before a deployer exists. Either way this jar reads one
# contract and does not know which side filled it.
#
# THE TRIPLE HAS NO DEFAULTS AND THAT IS THE REFUSE-TO-BOOT STANCE. An unset variable leaves these
# expressions unresolvable, so the process dies at Flyway's migrate-at-start naming the missing
# variable rather than opening some fallback store nobody meant. There is no local file to fall back
# to any more, and for this service the stake is the highest on the platform: an idp that came up
# against a store nobody meant would generate a NEW signing key and invalidate every token in
# flight, everywhere, with nothing in any log to say why.
quarkus.datasource.idp.db-kind=postgresql
quarkus.datasource.idp.jdbc.url=${QITS_RESOURCE_DB_URL}
quarkus.datasource.idp.username=${QITS_RESOURCE_DB_USERNAME}
quarkus.datasource.idp.password=${QITS_RESOURCE_DB_PASSWORD}

# THE POOL SURVIVES A POSTGRES CUTOVER, WHICH STOCK AGROAL DOES NOT. Replacing the tier's postgres
# severs every pooled connection; without validation the pool hands a dead one to the next caller
# long after the database is back, and 5s of acquisition is not enough for the successor to take the
# alias. Validate at borrow, and wait 15s before giving up.
#
# The driver is the third line of the platform's baseline and the three do less apart than they read
# as. PatientPgDriver (qits-db-core) delegates to org.postgresql.Driver and, when the database is not
# there, keeps asking for up to 14s instead of failing at once — it retries a refusal and postgres'
# "the database system is starting up", and fails a wrong password on the first attempt.
# validate-on-borrow is what turns a dead pooled connection into the fresh creation attempt it can be
# patient about; the 15s acquisition timeout outlasts its deadline, so a caller that waits it out
# gets the database's real refusal rather than a generic pool message. The url stays a plain
# jdbc:postgresql: one, so the QITS_RESOURCE_DB_URL contract above is untouched. It matters more here
# than almost anywhere: every service's token request comes through this one, so a cutover that fails
# them fails the platform's whole call graph rather than one application.
# `DatasourceBaselineTest` fails the build if a line goes missing; the measurements are in the
# superproject's docs/project-setup-quinoa-angular.md.
quarkus.datasource.idp.jdbc.driver=eu.wohlben.qits.db.PatientPgDriver
quarkus.datasource.idp.jdbc.acquisition-timeout=15S
quarkus.datasource.idp.jdbc.validate-on-borrow=true

# A dedicated persistence unit bound to the idp datasource, owning only this module's entities.
quarkus.hibernate-orm.idp.datasource=idp
quarkus.hibernate-orm.idp.packages=eu.wohlben.qits.idp.entity

# Own Flyway lineage at a distinct classpath location. ONE lineage: the move to postgres is a clean
# start, so there is a single V1 and no history inherited from the H2 lineage.
#
# baseline-on-migrate is GONE with that lineage. It told Flyway to adopt a non-empty schema it had
# no history table for, which the H2 file needed while it was being adopted; every database this
# code now meets is either empty or was created by this very lineage, and the flag would only mask
# the one case worth failing on — a schema somebody else put there.
quarkus.flyway.idp.migrate-at-start=true
quarkus.flyway.idp.locations=classpath:db/idp/migration

# --- the issuer, and the address, which are two values ---------------------------------------------
#
# THE ISSUER IS AN IDENTIFIER AND NOT AN ADDRESS. It is the `iss` of every token and the `issuer`
# member of the discovery document, and a consumer rejects a token whose `iss` differs from the
# document's by one character. Nothing dials it.
#
# It keeps this spelling deliberately. `qits-platform-idp` was a real address until the platform
# plane was deleted and every service moved to `<env>-<app>`; the issuer did not move with it,
# because a string compared for equality cannot be covered by a second DNS alias and cannot hold
# two values at once — changing it rejects every token in flight. It moves when every consumer is
# discovering rather than pinning, and not before. A deployment whose consumers sit elsewhere
# overrides it (QITS_IDP_ISSUER), and then every consumer must be told the same string: an issuer
# mismatch is rejected at validation, which is the intended loud failure.
qits.idp.issuer=http://qits-platform-idp:8080/idp

# THE ENDPOINT BASE IS AN ADDRESS AND IS DIALLED. The discovery document hangs token_endpoint,
# jwks_uri and authorization_endpoint off it.
#
# It used to be the issuer, which was right while the two were the same string and became a defect
# the moment they parted: the document went on advertising a jwks_uri on the bare alias, so a
# consumer booting with nothing cached followed it to an UnknownHostException and rolled back,
# while every already-running consumer carried on from its cache. A working platform hid it.
#
# Derived from the environment rather than carried as a deployment config entry: an address that
# follows one rule is code. A config row holding it is a second copy of that rule, and the second
# copy is the one that goes stale without saying so.
qits.idp.endpoint-base=${QITS_IDP_ENDPOINT_BASE:http://${QITS_ENVIRONMENT:dev}-qits-platform-idp:8080/idp}

# How long an issued token is good for.
#
# RAISED FROM 300 TO 3600 ON 2026-08-14, a user decision recorded with the commission model
# (authenticated-reads-plan.md, "the credential model"). What changed is that the LONG-LIVED thing
# is now the commissioned id+secret pair, not the token: a workspace that runs for days holds the
# pair and re-mints underneath itself, so a context's lifetime no longer depends on a token's. The
# TTL surfaces in exactly two places after that — a consumer wired with a bare minted token instead
# of the pair, and the post-decommission grace — and the raise exists to put the first class out of
# scope for now rather than design refresh/recommissioning on the way past it.
#
# THE NAMED COST, ACCEPTED: the grace widens to the same hour. Decommissioning is deleting the row,
# which stops the client minting IMMEDIATELY — but a token it minted a second earlier keeps working
# until its own `exp`, everywhere, because validation is offline against the JWKS and there is no
# revocation list. Revisit this number together with per-context permission scoping; shrinking it
# again is this one key.
qits.idp.token-ttl-seconds=3600

# --- local Git workstations ----------------------------------------------------------------------
#
# A local workstation is a PKCE-only public OAuth client, never a service client.  Its access token
# is short lived, has only the githost audience and the qits:git:external role, and carries the
# ref-pattern claim the githost enforces.  The durable credential is the opaque refresh token: it
# rotates on every use and can be revoked per workstation family from the signed-in account.
qits.idp.workstation.client-id=qits-git-workstation

# The githost audience follows QITS_ENVIRONMENT, which the deployer gives every app, because the
# githost's wire alias does: `prod-qits-githost` on prod, `dev-qits-githost` on dev. /authorize
# refuses any other audience, so a fixed value would lock Git sign-in out of every other
# environment. With no QITS_ENVIRONMENT it is prod.
qits.idp.workstation.githost-audience=${QITS_ENVIRONMENT:prod}-qits-githost
qits.idp.workstation.access-token-ttl-seconds=900
qits.idp.workstation.authorization-code-ttl=PT2M
qits.idp.workstation.refresh-token-ttl=PT720H

# --- the command-line tool ------------------------------------------------------------------------
#
# The second public client. Same protocol as the workstation above — authorization code with PKCE
# S256, no secret, a rotating refresh family that replay revokes — and three differences:
#
#   * THE REDIRECT TARGET MAY BE THIS IDP'S OWN PAGE. `qits login` has no browser to catch a
#     loopback redirect at the moment the person finishes signing in, so the idp shows the code on
#     <origin>/idp/connect/cli and the person pastes it into the waiting command. PKCE is what makes
#     that safe: the pasted code is useless without the verifier the waiting process holds, so a
#     code that leaks through a clipboard or a screen share cannot be spent. Loopback stays allowed
#     for a CLI that does have a listener.
#
#   * THE TOKEN CARRIES THE PERSON'S OWN ROLES, not one fixed narrow role. The CLI is the person at
#     a terminal, so it is as strong as the browser session they just signed in with; narrowing it
#     to a project or a service is named later work. What holds the blast radius is the 900-second
#     access token, rotation with replay detection, and revoke-per-device.
#
#   * THE CODE LIVES FIVE MINUTES, not two. A loopback redirect is caught in milliseconds; a person
#     reading a code off a page, switching to a terminal and pasting it is not. Two minutes is a
#     deadline a distracted person misses, and missing it looks like a broken login.
qits.idp.cli.client-id=qits-cli

# WHAT A CLI TOKEN MAY BE SPENT AGAINST: one audience, `qits-platform` (user ruling 2026-09-12).
#
#   * ONE AUDIENCE ON PURPOSE. A person's CLI may call every qits service. The token's roles
#     (`groups`) are the permission: RBAC decides what the person may do, not the audience.
#   * NO ENVIRONMENT PREFIX ON PURPOSE. It names the platform, not one service in one environment,
#     so it is the same on every installation and needs no override.
#   * THE EDGE accepts it on every vhost (qits.edge.auth.platform-audience), and each service lists
#     it in quarkus.oidc.token.audience.
qits.idp.cli.audiences=qits-platform

# Fifteen minutes, the same as a workstation's: a stolen access token is unrevocable until it
# expires, because validation is offline against the JWKS.
qits.idp.cli.access-token-ttl-seconds=900

# See the five-minute note above.
qits.idp.cli.authorization-code-ttl=PT5M

# There is deliberately no qits.idp.cli.refresh-token-ttl: the CLI's families expire on
# qits.idp.workstation.refresh-token-ttl above. The number answers "how long may a person stay
# signed in on a device they still hold", which is a fact about the person and the installation
# rather than about which tool is asking — and two keys would be two values to drift apart, with
# "my CLI logged out but my Git did not" as the thing somebody then has to explain. Split it on the
# day the two answers genuinely differ. WorkstationCredentials says the same at the field.

# The RSA modulus size for a key this service GENERATES. Existing keys are used as they are stored,
# so changing this rotates nothing by itself.
qits.idp.signing-key-bits=2048

# --- user sessions --------------------------------------------------------------------------------
#
# How long a browser session lives, from the moment it is opened. ABSOLUTE, not idle: a session dies
# twelve hours after login whatever the user did in between, because sliding renewal is a named open
# question in user-authentication-plan.md and not a blocker — logging in again is one ceremony.
#
# Twelve hours is a working day plus the evening. Long enough that an operator is not asked to
# re-authenticate mid-task, short enough that a browser left open on a machine somewhere has stopped
# being a way in by the next morning. A deployment overrides it (QITS_IDP_SESSION_TTL) with anything
# java.time.Duration parses; there is nothing about the number this repository knows better than an
# installation does.
#
# It is the deadline stamped on the idp_session row AND the cookie's Max-Age, from one value, so a
# browser never holds a cookie the store has already stopped honouring.
qits.idp.session-ttl=PT12H

# --- static clients ------------------------------------------------------------------------------
#
# The long-lived service clients — STATIC IDENTITIES, for the platform services themselves, whose
# identity genuinely is static. Config is the whole registry for them.
#
# The other half of the registry is rows: COMMISSIONED clients, one per dynamic context, created
# through POST /idp/api/clients by the service that provisioned the context and deleted when it
# ends. They are not configured and cannot be — see the idp_client table and DynamicClients. Two
# rules connect the halves: config is resolved FIRST, so a row can never shadow a service client;
# and only a client listed below may commission, so a commissioned credential cannot produce more
# of itself.
#
#   qits.idp.clients                        the client ids that exist, comma-separated. A
#                                           `client_id` that is not on this list is unknown, and an
#                                           unknown id never reaches a config lookup — which is what
#                                           keeps an attacker-supplied id out of the key namespace.
#   qits.idp.client.<id>.secret             the shared secret. ABSENT OR BLANK MEANS UNUSABLE, never
#                                           open: a client with no secret is refused
#                                           `invalid_client` like a wrong one. That is why no secret
#                                           is shipped below — a default secret would be a default
#                                           credential.
#   qits.idp.client.<id>.audiences          the `aud` values this client may ask for,
#                                           comma-separated. A request naming none gets ALL of them;
#                                           a request naming one outside the list is
#                                           `invalid_target`.
#   qits.idp.client.<id>.claims.<name>      a granted structured claim, copied into the token
#                                           verbatim. <name> is one of project, workspace, branch —
#                                           the contract's whole set; there are no scope strings.
#                                           `*` is a value like any other here: the idp states it,
#                                           and the resource service's own enforcement decides that
#                                           it matches everything. It is also the value ONLY this
#                                           file may set — a commission cannot ask for it.
#
# Every key above is env-overridable by the usual mangling —
# QITS_IDP_CLIENT_PROD_QITS_CI_SECRET, QITS_IDP_CLIENT_PROD_QITS_CI_AUDIENCES,
# QITS_IDP_CLIENT_PROD_QITS_CI_CLAIMS_PROJECT — and QITS_IDP_CLIENTS adds a client that is in no
# file.
#
# A COMMISSIONED CLIENT IS ISSUED ITS OWNER'S audiences, read from the owner's lines above when a
# token is minted rather than copied into the row: a credential qits-ci commissioned can be used
# where qits-ci can be used. So narrowing a client's audiences here narrows every credential it
# commissioned, at once.
#
# ITS ROLES ARE NOT THE OWNER'S (service-client-identity-plan.md, D3/D12). A commission's roles are
# fixed in code by its context kind — CommissionRoles — never configured here and never inherited:
# `workspace`, `agent-container` and `refinement` get `qits:agent`; `ci-run` and
# `bootstrap-publish` get `qits:ci-run` — publishing to qits-artifacts is CI's door, and
# `bootstrap-publish` is the identity the bootstrap commissions for its own publish phase and
# deletes when that phase ends; any
# other kind, including one this jar has never heard of, gets no role at all beyond its own
# `clients/<id>`. There used to be a `qits.idp.commission.roles.<kind>` key here; it is gone, along
# with the deployment override — a kind's role is no longer a thing a deployment can change.
#
# ITS CLAIMS ARE NOT THE OWNER'S EITHER (D3). A commission may state `claims` —
# {"project":"<projectId>"} for a workspace — and those, and only those, go into every token that
# credential mints. `*` is the one value a commission may NOT state (400): a concrete value is
# narrower than saying nothing, while the wildcard is never a narrowing, so it stays a service
# client's own grant below. A commission that states nothing carries no claims at all — there is no
# owner grant left to fall back to.
#
# AN ID IS PART OF THE KEY. Renaming a client means moving its `qits.idp.client.<id>.*` lines with
# it — a line left behind under the old id is read by nothing and says nothing about it.
#
# The ids are the names a service is DIALED by. A platform service is its repository name
# (qits-platform-artifacts); an environment service carries its environment
# (prod-qits-ci). The deployer mints nothing, so it is no client here — it is the audience
# `qits-deployments` below, because qits-ci presents a token to its intake.
#
# `qits-deployments` HAS NO PREFIX because the deployer became a platform service, one instance
# across every environment: its wire alias lost the tier and the audience follows it, since the two
# are the same string. The repository is still named qits-deployments, so that is the name; a later
# rename to qits-platform-deployments moves the audience with it, on both sides in one step.
qits.idp.clients=prod-qits-ci,qits-platform-artifacts,prod-qits-workspaces

# The audience lists ship WIDE — every service client may ask for any platform audience — and that
# is a decision. Phase 1's job is to make identity assertable, not to narrow a call graph nobody has
# enumerated yet; a too-tight default fails a service adoption with `invalid_target` on a path no
# test here covers. Narrowing is a deployment change (QITS_IDP_CLIENT_<ID>_AUDIENCES) and should
# happen once the call graph is real. What the lists already do is bound the blast radius of a
# leaked secret to the platform's own services.
#
# The lists are LONGER than the client list on purpose: qits-deployments is an audience with no
# client, because it receives tokens and mints none.
qits.idp.client.prod-qits-ci.audiences=prod-qits-ci,qits-platform-artifacts,prod-qits-workspaces,qits-deployments,prod-qits-githost
qits.idp.client.qits-platform-artifacts.audiences=prod-qits-ci,qits-platform-artifacts,prod-qits-workspaces,qits-deployments,prod-qits-githost
qits.idp.client.prod-qits-workspaces.audiences=prod-qits-ci,qits-platform-artifacts,prod-qits-workspaces,qits-deployments,prod-qits-githost

# EVERY CLIENT TOKEN ALSO CARRIES `clients/<its own id>`, stamped into `groups` at mint time from
# the id that authenticated. It is not configured here and it cannot be: a role naming one client is
# held by that client alone, which is what lets a resource service gate a route with
# @RolesAllowed("clients/prod-qits-projects") and know exactly one caller can ever reach it. So the
# `clients/` namespace is RESERVED — a roles line containing one is refused (invalid_request), for
# another client's id and for the client's own alike, and the client mints nothing until it is
# removed. A commissioned credential gets its own `clients/dyn-…` the same way and never its
# owner's, so it cannot walk through its owner's door.
qits.idp.client.prod-qits-ci.roles=qits:system
qits.idp.client.qits-platform-artifacts.roles=qits:system
qits.idp.client.prod-qits-workspaces.roles=qits:system

# No `claims` are granted here, and no secrets. A deployment that wants qits-platform-artifacts'
# post-receive hook to reach qits-ci's event intake grants it the claim the intake enforces —
# QITS_IDP_CLIENT_QITS_PLATFORM_ARTIFACTS_CLAIMS_PROJECT='*' — and gives it a secret. Both are
# deployment facts; neither has a defensible default in a repository.
#   qits.idp.client.<id>.roles              machine roles copied into the JWT `groups` claim.
#                                           Every platform client receives the one coarse system
#                                           role; resource services still check `aud` separately.
#                                           `clients/…` is reserved — see the block above the roles
#                                           lines: the idp mints that one itself.

# --- service clients in the database ---------------------------------------------------------------
#
# The database half of the client registry (service-client-identity-plan.md, contract C2):
# `idp_service_client` rows, created and rotated through `/idp/api/service-clients` rather than
# configured here. A row's roles, claims and audience rule are code — see ClientRegistry — never
# configuration, so there is nothing to ship a key reference for beyond the one seam that IS
# configuration: the very first row, which nothing can create through the API because nothing can
# authenticate to it yet.
#
#   qits.idp.seed-client.id       the id of the one service client this process seeds at first boot.
#   qits.idp.seed-client.secret   its secret, hashed on the way into idp_service_client and never
#                                 read back.
#
# QITS_IDP_SEED_CLIENT_ID / QITS_IDP_SEED_CLIENT_SECRET are the env spellings, and both are unset by
# default — no default seed identity ships, the same "no secret, no identity" rule every static
# client already follows. SEEDING RUNS AT MOST ONCE PER INSTALLATION: the `idp_seed` marker row, not
# the variables, decides "once", so leaving the variables set on every later boot (the ordinary
# case) seeds nothing a second time, and a deleted seed client does not come back by restarting the
# process that made it.
#
# A CLIENT WITH A DATABASE ROW CAN STILL HAVE AN ENVIRONMENT ENTRY ABOVE, transitionally: creating a
# database row for an id that is already on `qits.idp.clients` is not refused (201), and while both
# exist either secret authenticates. The environment entry keeps deciding that id's roles, claims
# and audiences until it is removed; only a purely database-backed id gets the roles and claims this
# file no longer configures.
